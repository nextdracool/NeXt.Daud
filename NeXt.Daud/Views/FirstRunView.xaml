<UserControl x:Class="NeXt.Daud.Views.FirstRunView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NeXt.Daud.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Margin="2">
            <TextBlock Text="Make sure you have normal settings (no speedrun bindings, no daud%)" TextWrapping="Wrap" Foreground="Red"/>
            
        </StackPanel>
        
        <ContentControl Grid.Column="1" x:Name="Updater"/>
    </Grid>
</UserControl>
